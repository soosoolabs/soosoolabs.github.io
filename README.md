# gh-pages — 옛 주소(soosoolabs.github.io)를 soosoo.kr 로 넘기는 껍데기

2026-09-20 부터 수수 홈페이지의 정본은 **https://soosoo.kr** (Cloudflare · `main` 브랜치에서 `npm run deploy`).
이 브랜치는 스토어·약관 문서에 이미 박힌 옛 주소가 죽지 않도록 **같은 경로로 넘겨주기만** 한다.
⛔ 여기에 내용을 넣지 말 것. 경로가 늘면 `main` 의 `data/site.json` fixed_paths 와 같이 껍데기를 추가한다.
