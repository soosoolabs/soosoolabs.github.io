{
  "name": "soosoo-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "check": "node tools/check.mjs",
    "check:selftest": "node tools/check.mjs --selftest",
    "preview": "npx --yes serve dist -l 4173",
    "deploy": "npm run build && npm run check && npx wrangler deploy",
    "deploy:check": "node tools/deploy-check.mjs",
    "fonts": "node tools/fonts.mjs"
  }
}
